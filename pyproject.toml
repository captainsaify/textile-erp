[project]
name = "textile-erp"
version = "0.1.0"
description = "WhatsApp-native trading ERP backend"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "gunicorn>=23.0",
    "sqlalchemy[asyncio]>=2.0.36",
    "asyncpg>=0.30",
    "alembic>=1.14",
    "pydantic[email]>=2.9",
    "pydantic-settings>=2.6",
    "structlog>=24.4",
    "redis>=5.2",
    "argon2-cffi>=23",
    "pyjwt>=2.9",
    "typer>=0.13",
    "httpx>=0.27",
    "rapidfuzz>=3.10",
    "opencv-python-headless>=4.10",
    "numpy>=2.1",
    "pytesseract>=0.3.13",
    "pillow>=11.0",
    "pdf2image>=1.17",
    "anthropic>=0.69",
    "celery[redis]>=5.4",
    "openpyxl>=3.1.5",
    "agentops>=0.4.21",
]

[dependency-groups]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
    "pytest-cov>=6.0",
    "ruff>=0.7",
    "mypy>=1.13",
    "pyyaml>=6.0.3",
    "types-pyyaml>=6.0.12.20260724",
    "httpx>=0.28.1",
]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
# Money/quantity fields must never be float -- see docs/17_CodingStandards.md §1.
# (Mechanical enforcement of this rule is a documented follow-up; reviewed by hand for now.)

[tool.ruff.lint.per-file-ignores]
# autogenerated by alembic; long lines and legacy typing come from the generator
"alembic/versions/*" = ["E501", "UP007", "UP035", "I001"]

[tool.mypy]
python_version = "3.12"
strict = true
plugins = ["pydantic.mypy"]
exclude = ["alembic/versions"]

# OCR engines ship no type stubs; their call sites are narrowly wrapped
# in backend/ocr/engines.py and guarded at runtime.
[[tool.mypy.overrides]]
module = ["pytesseract.*", "paddleocr.*", "pdf2image.*", "cv2.*"]
ignore_missing_imports = true

# celery and openpyxl ship no type information; both are used through
# narrow wrappers (backend/workers/, backend/reports/excel/) whose own
# signatures are fully typed.
[[tool.mypy.overrides]]
module = ["celery.*", "openpyxl.*"]
ignore_missing_imports = true
disallow_untyped_decorators = false

# agentops ships no py.typed. It is reached only through
# backend/core/observability.py, which is fully typed and swallows every
# failure the SDK can produce.
[[tool.mypy.overrides]]
module = ["agentops.*"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = ["backend.workers.*"]
disallow_untyped_decorators = false

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["backend/tests"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["backend"]
